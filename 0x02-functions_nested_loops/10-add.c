#include <stdio.h>
#include "main.h"
/**
 * add - function to add two numbers
 * @a: first number
 * @b: second number
 * Return: Returns 0
 */
int add(int a, int b)
{
	int sum = a + b;
	return (sum);
}
