#include <stdio.h>
#include "main.h"
/**
 * _isdigit - function
 * @a: first integer
 * @b: second integer
 * Return: returns multiplication of two integers
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
